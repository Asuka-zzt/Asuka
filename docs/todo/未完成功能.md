# 未完成功能清单（TODO）

> 记录 MVP 各阶段为求最快验收而**跳过或简化**的功能，避免遗忘。
> 每项标注：来源阶段、当前简化做法、补全时机。

---

## 来自 Phase 1（脚手架）

| 功能 | 当前做法 | 补全时机 |
|---|---|---|
| 数据库层 `db/` | 完全未建 | Phase 需要持久化 Agent/会话时 |
| `api/platform/`（IM 适配） | 未建 | 接入 QQ/微信等平台时 |
| `api/event/`（事件总线） | 未建 | 多模块解耦需要时 |

---

## 来自 Phase 2（最小可对话 Agent）

| 功能 | 当前简化做法 | 补全时机 |
|---|---|---|
| **多 Agent 并发** | 单 Agent，`create_react_agent` | Phase 3 之后，改 `StateGraph` + `Send` |
| **session/manager.py** | 用 `conversation_id` 直接当 `thread_id` | 需管理多会话生命周期 / 内存中 Session 池时 |
| **session/context.py** | 不构造独立 Context | 同上 |
| **Agent 配置持久化** | 硬编码 1 个 default agent，不接 DB | DB 层就绪 + Agent CRUD 时 |
| **core/agent/registry.py** | 未建（只有一个写死的 config） | 支持多 Agent 配置加载时 |
| **core/agent/persona.py** | soul 直接写在 AgentConfig 字段里 | 人格模板 / 多段 Prompt 组装时 |
| **core/agent/runtime.py** | build_agent() 直接返回，无 Runtime 封装 | 引入 session 层时 |
| **Prompt 可视化 / snapshot** | 无，prompt 直接交给 create_react_agent | Phase: Prompt 可视化模块 |
| **中期记忆（摘要）** | 无，只靠 checkpointer 全量历史 | 上下文增长需压缩时 |
| **长期记忆 / RAG** | 无 | 知识库功能开发时 |
| **多 provider 完整支持** | 只实测 DeepSeek；gpt/claude 分支写了未验证 | 需要切换模型时逐个验证 |
| **错误处理 / 重连** | WebSocket 无断线重连、无异常下发 | 稳定性增强阶段 |

---

## 来自 Phase 3（Wiki 生成工具 — LangGraph 子图）

| 功能 | 当前简化做法 | 补全时机 |
|---|---|---|
| 插件动态加载 | `registry.py` 写死注册 `generate_wiki` | 插件系统开发时 |
| 工具沙箱隔离 | 直接执行，无 sandbox | 接入第三方/不可信工具时 |
| 数据源 | 仅本地目录（`fs.collect_files`），无 GitHub 远程抓取 | 需分析远程仓库时 |
| 章节间上下文 | `Send` 并行写章，丢弃"已写章节摘要"（每章仅有全章目录+前后邻居）| 需更强跨章连贯性时改顺序 reduce |
| LLM 响应缓存 | 无（参考里的 use_cache 未移植） | 重复生成、控本成本时 |
| identify/analyze 上下文 | 整库/引用文件入单 prompt，靠 `max_file_size` 限流 | 超大项目溢出时改 map-reduce 分批 |
| 工具进度下发 | WebSocket 仅在工具结束后流式总结，无中间进度 | 需实时进度时加自定义事件 |
| structured_output 兼容 | 依赖 provider 的 function-calling/json 模式（DeepSeek 待实测）| 不兼容时加 YAML 兜底解析 |

---

## 跨阶段（整体）

| 功能 | 状态 | 补全时机 |
|---|---|---|
| Vue 前端 + Live2D | 未启动 | 后端 API 稳定后 |
| 插件系统 `core/plugins/` | 未建 | 工具需热插拔时 |
| 知识库 `core/knowledge/` | 未建 | RAG 阶段 |
| 鉴权 / 多用户 | 无 | 部署 / 多用户场景 |
| PostgreSQL 切换 | 只用 SQLite | 云端部署时 |
