# 前端设计 — Stage 式界面（参考 AIRI，对齐后端现有能力）

> 目标：搭一套 Vue 3 前端，主视图采用 AIRI 式「左舞台 / 右聊天」布局，
> **为 Live2D 人物预留独立舞台空位**（占位组件，后续挂 Pixi 画布）；
> 前端只暴露**后端当前真实提供的能力**，不为尚不存在的接口造占位页。
>
> 参考 `ref/normal_agent/airi`：
> - 布局：`apps/stage-web/src/pages/index.vue`（背景 → Header → 左 `WidgetStage` / 右 `InteractiveArea`）
> - Live2D：`packages/stage-ui-live2d/src/components/scenes/live2d/Canvas.vue`（透明 Pixi 画布挂容器）
> - 聊天：`packages/stage-layouts/.../InteractiveArea.vue`（ChatHistory + ChatArea + Pinia stores）

---

## 0. 后端能力清单（前端对齐目标）

| 后端接口 | 形态 | 前端对应 |
|---|---|---|
| `GET /health` → `{status, version}` | REST | Header 连接状态徽标（在线点 + 版本号） |
| `WS /ws/{conversation_id}` 流式 | WebSocket | **Chat 视图**：流式逐字对话 |
| `POST /chat {conversation_id, message}` → `{reply}` | REST 非流式 | **Agent 测试台**：表单发一条、看完整回复 |
| `generate_wiki` 工具（Agent 内置） | 经对话触发 | 在 Chat 里自然语言触发（"给 X 生成 wiki"）|
| 默认 Agent（`id/name/soul/model_id` 硬编码，**无 CRUD 接口**） | 启动时固定 | Agent 测试台**只读展示**默认人格 |

> 没有 Agent CRUD / Prompt 快照 / 知识库接口 → 本期**不做**这些视图（仅在 §12 登记为后续）。

---

## 1. 范围

**做：**
- Vue 3 + TS + Vite + pnpm + Pinia + vue-router + **UnoCSS**，独立 `frontend/` 目录。
- AIRI 式 `StageLayout`：全屏背景 → Header（含 `/health` 徽标）→ 左**舞台**（Live2D 空位）/ 右**聊天面板**，移动端竖排。
- **Chat 视图（`/`）**：连 `WS /ws/{conversation_id}`，流式 token 渲染、多轮显示；可自然语言触发 `generate_wiki`。
- **Agent 测试台（`/agent`）**：只读展示默认 Agent（name/soul/model_id），下方表单接 **`POST /chat`** 非流式发送、显示 `reply`。
- **Live2DStage 空位组件**：填满左舞台，MVP 显示占位，留好未来挂 `pixi-live2d-display` 的接缝。

**不做（登记 TODO）：**
- Live2D 真实渲染（仅留空位与 composable 接口）。
- Prompt 可视化 / 知识库视图（后端无对应接口）。
- Agent 配置**持久化**（后端无 CRUD，表单字段只读展示，不可保存）。
- 多 Agent 并发气泡（后端当前单 Agent；`Message.agentId` 预留字段不渲染多栏）。
- WS 断线指数退避、Electron 打包、鉴权多用户。

---

## 2. 技术栈

| 维度 | 选型 | 说明 |
|---|---|---|
| 框架 | Vue 3 `<script setup>` + TS | 与 AGENTS.md 一致 |
| 构建 | Vite | dev proxy 转发 `/ws`、`/chat`、`/health` 到后端 |
| 包管理 | **pnpm**（独立于后端 uv） | 环境无 pnpm 时 `corepack enable pnpm` |
| 状态 | Pinia | 会话 / 后端健康 / UI 偏好 |
| 路由 | vue-router | Chat / Agent 两视图 |
| 样式 | **UnoCSS** | 与 AIRI 原子化 class 一致 |
| 图形（后续） | `pixi.js` + `pixi-live2d-display/cubism4` | 本期不装，仅预留接缝 |

---

## 3. 目录结构

```
frontend/
├── index.html
├── package.json
├── vite.config.ts            # Vue + UnoCSS 插件 + dev proxy(/ws,/chat,/health → 127.0.0.1:8000)
├── uno.config.ts             # UnoCSS 预设（参考 AIRI，精简）
├── tsconfig.json
├── .env.example              # VITE_WS_BASE / VITE_API_BASE
└── src/
    ├── main.ts               # createApp + pinia + router + uno reset
    ├── App.vue               # <RouterView/>
    ├── router/index.ts       # 路由表（/ , /agent）
    ├── layouts/
    │   └── StageLayout.vue    # ★ AIRI 式：背景 + Header + 左舞台槽 / 右面板槽（具名 slot）
    ├── components/
    │   ├── layout/
    │   │   ├── AppHeader.vue          # 顶栏：标题 + 导航(Chat/Agent) + 健康徽标
    │   │   └── HealthBadge.vue        # GET /health 轮询：在线点 + 版本
    │   ├── stage/
    │   │   ├── Live2DStage.vue        # ★ Live2D 空位容器（填满左舞台，留挂载接缝）
    │   │   └── StagePlaceholder.vue   # 占位视觉（虚线框 + 角色剪影/提示）
    │   └── chat/
    │       ├── ChatPanel.vue          # 对应 InteractiveArea：容器
    │       ├── ChatHistory.vue        # 消息列表（含流式消息）
    │       ├── MessageBubble.vue      # 单条消息（预留 agentId / 人格色）
    │       └── ChatInput.vue          # 输入框 + 发送 + 连接状态
    ├── composables/
    │   ├── useChatSocket.ts           # 封装 /ws：连接 / 发送 / token 流式 / 基础重连
    │   └── useLive2D.ts               # Live2D 挂载接口（本期 stub，留 TODO）
    ├── stores/
    │   ├── chat.ts                    # messages + 流式拼接 + sending/connected
    │   ├── health.ts                  # 后端 status/version + online
    │   └── ui.ts                      # 主题 / 当前视图
    ├── api/
    │   └── client.ts                  # fetch 封装：health()、postChat()
    ├── views/
    │   ├── ChatView.vue               # 主视图：StageLayout(左 Live2DStage / 右 ChatPanel)
    │   └── AgentTestView.vue          # Agent 测试台：默认人格(只读) + POST /chat 表单
    ├── types/
    │   └── chat.ts                    # Message / WsEvent / Agent 类型
    └── styles/main.css
```

---

## 4. 布局设计（AIRI 映射）

桌面端（参考 `index.vue`：左 `flex-1 min-w-1/2`，右面板 `max-w-500px`）：

```
┌──────────────────────────────────────────────────────────────┐
│  AppHeader   AsukaBot   [Chat][Agent 测试台]     ● v0.x 在线   │
├───────────────────────────────────┬──────────────────────────┤
│                                    │  ChatPanel                │
│        Live2DStage（空位）          │  ┌──────────────────────┐ │
│   ┌───────────────────────────┐    │  │ ChatHistory          │ │
│   │   ╭─────────╮              │    │  │  Asuka: 你好…(流式)   │ │
│   │   │  🎭     │  占位剪影     │    │  │  我: …(右对齐)        │ │
│   │   ╰─────────╯              │    │  └──────────────────────┘ │
│   │  "Live2D 区域 · 待接入"     │    │  ChatInput [输入…] [发送] │
│   └───────────────────────────┘    │  ● 已连接 / ○ 重连中       │
└───────────────────────────────────┴──────────────────────────┘
```

移动端（`<md` 切 `flex col`）：舞台缩为顶部条 + 聊天占满。

`StageLayout.vue` 用具名 slot 解耦，主视图：

```vue
<StageLayout>
  <template #stage><Live2DStage /></template>
  <template #panel><ChatPanel /></template>
</StageLayout>
```

Agent 测试台不用舞台，走纯内容页（无 stage slot）。

---

## 5. Live2D 空位设计（关键）

`Live2DStage.vue` —— **预留并填满左舞台，本期占位，未来挂 Pixi 画布**：

```vue
<script setup lang="ts">
import { onMounted, ref } from 'vue'
import StagePlaceholder from './StagePlaceholder.vue'
import { useLive2D } from '@/composables/useLive2D'

defineProps<{ modelUrl?: string }>()
const stageRef = ref<HTMLDivElement>()
const { state } = useLive2D(stageRef)   // 本期 state 恒为 'placeholder'

onMounted(() => {
  // 本期：不挂载，显示占位。
  // 未来：modelUrl 存在时 mount(modelUrl) —— 内部创建透明 Pixi Application
  //       (backgroundAlpha:0)，把 canvas 以 width/height:100% objectFit:contain 填入 stageRef，
  //       参考 AIRI live2d/Canvas.vue。
})
</script>

<template>
  <!-- 这个容器就是 Live2D 的「空位」：透明、填满左舞台、未来 canvas 挂这里 -->
  <div ref="stageRef" relative h-full w-full flex items-center justify-center of-hidden>
    <StagePlaceholder v-if="state === 'placeholder'" />
  </div>
</template>
```

`useLive2D.ts`（本期 stub，划清接缝）：

```ts
export type Live2DState = 'placeholder' | 'loading' | 'mounted' | 'error'

export function useLive2D(container: Ref<HTMLDivElement | undefined>) {
  const state = ref<Live2DState>('placeholder')
  // 未来：动态 import pixi.js + pixi-live2d-display/cubism4，
  // new Application({ backgroundAlpha: 0 })，Live2DModel.from(modelUrl)，appendChild(view)。
  async function mount(_modelUrl: string) { /* TODO(live2d) */ }
  function dispose() { /* TODO(live2d) */ }
  return { state, mount, dispose }
}
```

设计要点：透明容器（与 `backgroundAlpha:0` 同思路），尺寸由布局撑开，Pixi 画布 `objectFit:contain` 自适应；
接入真模型只差 `useLive2D.mount()` 实现 + 装两个依赖，**布局与调用方不变**。

---

## 6. 路由与视图

| 路径 | 视图 | 后端依赖 | 说明 |
|---|---|---|---|
| `/` | `ChatView` | `WS /ws/{id}` | 左 Live2D 空位 / 右流式聊天；可触发 `generate_wiki` |
| `/agent` | `AgentTestView` | `POST /chat` | 默认人格只读展示 + 非流式发送测试 |

### 6.1 Agent 测试台（`POST /chat`）

```
┌─ 默认 Agent（只读，来自后端硬编码）──────────────┐
│  名称: Asuka    模型: deepseek-chat              │
│  Soul: 你是 Asuka，一个友好、简洁…（多行只读）     │
│  ⓘ 后端暂无 Agent 配置接口，字段不可保存（待 DB 层）│
└──────────────────────────────────────────────┘
┌─ 非流式测试（POST /chat）─────────────────────────┐
│  会话ID: [test-conv      ]                        │
│  消息:   [给 ref/... 生成 wiki        ] [发送]     │
│  回复:   ┌────────────────────────────────────┐  │
│          │ {reply 完整文本}                     │  │
│          └────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
```

- 默认 Agent 字段为前端已知常量（后端无 `GET /agent`），标注「只读 / 不可保存」，对齐后端真实情况。
- 「发送」→ `POST /chat {conversation_id, message}` → 展示 `reply`；与 Chat 视图的 WS 流式互为对照（一个流式、一个整段），覆盖后端两个对话接口。

---

## 7. 状态管理（Pinia）

`stores/chat.ts`（核心）：

```ts
interface Message {
  id: string
  role: 'user' | 'assistant'
  agentId?: string          // 预留多人格；当前恒为 'default'
  content: string
  streaming?: boolean
}
state:   { conversationId, messages: Message[], sending, connected }
actions: send(text) / appendToken(token) / finalize() / setError(msg)
```

- `health.ts`：轮询 `GET /health`，存 `online` / `version`。
- `ui.ts`：主题 / 当前视图。

---

## 8. 与后端对接

### 8.1 WebSocket（`routes/ws.py`）

```
连接：ws://<host>/ws/{conversationId}
发送：{ "message": "用户输入" }
接收：{ "type":"token", "content":"增量" } | { "type":"done" } | { "type":"error", "content":"…" }
```

`useChatSocket.ts`：建连、`send(text)`、token→`chat.appendToken`、done→`finalize`、error→`setError`、基础重连。

### 8.2 REST（`routes/chat.py`、`main.py`）

```
POST /chat { conversation_id, message } → { reply }     # Agent 测试台
GET  /health → { status, version }                       # Header 徽标轮询
```

`api/client.ts` 封装两者。

### 8.3 Vite dev proxy（避免 CORS）

```ts
server: {
  proxy: {
    '/ws':     { target: 'ws://127.0.0.1:8000', ws: true },
    '/chat':   { target: 'http://127.0.0.1:8000' },
    '/health': { target: 'http://127.0.0.1:8000' },
  },
}
```

---

## 9. 数据流（一次流式对话）

```
ChatInput 输入 → chat.send(text)
  → push {role:user} + push {role:assistant, streaming:true}
  → useChatSocket.send({message:text})
  → WS ← {type:token} ×N → chat.appendToken → 气泡逐字增长
  → WS ← {type:done}     → chat.finalize    → 气泡定稿
  （触发 wiki 时：工具执行期间无 token，结束后摘要整段流回；多轮由后端 checkpointer 续上下文）
```

---

## 10. 验收标准

- ✅ `pnpm dev` 启动，`/` 显示 AIRI 式布局：左舞台 Live2D 空位（占位可见）/ 右聊天面板。
- ✅ 后端起着时，`/` 发消息看到**流式逐字**回复、多轮记忆生效；问"给 X 生成 wiki"能触发工具并返回摘要。
- ✅ `/agent` 展示默认人格（只读），表单 `POST /chat` 能发一条并显示完整 `reply`。
- ✅ Header 健康徽标随后端启停在「在线/离线」切换并显示版本。
- ✅ 移动端宽度下布局竖排不破版；Live2D 空位随窗口自适应；`useLive2D` 接口就位（mount 为 TODO 不报错）。
- ✅ `pnpm build` 通过；`pnpm lint` 无 error。

---

## 11. 风险与权衡

| 项 | 处理 |
|---|---|
| 默认 Agent 无 GET 接口 | 前端用已知常量只读展示，明确「不可保存」，不伪造持久化 |
| wiki 工具执行期无 token | UI 表现为短暂停顿后整段摘要；当前协议无法上报进度，登记 TODO |
| 多 Agent 并发 UI | `Message.agentId` 预留；当前单栏单 Agent |
| Live2D 依赖体积大 | 本期不装，仅留 composable 接缝，接入时按需动态 import |
| WS 断线 | 基础重连；指数退避 / 错误态 UI 细化登记 TODO |

---

## 12. 登记到 `docs/todo/未完成功能.md` 的项（实现时补登）

- Live2D 真实渲染（`useLive2D.mount` + pixi/live2d 依赖 + 模型加载/口型/动作）。
- Prompt 可视化 / 知识库视图（依赖后端对应 routes）。
- Agent 配置持久化（依赖 DB 层 + Agent CRUD）。
- 多 Agent 并发气泡布局（依赖多 Agent 后端）。
- WS 断线指数退避重连 + 错误态 UI；wiki 工具执行进度上报。
- Electron 桌面打包 / PWA 离线。
```

